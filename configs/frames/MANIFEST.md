Example SITL Frame Bundles
==========================

These checked-in examples are ready to mount as `SITL_CONFIG_DIR`.

| Bundle | Vehicle | Frame | Copied assets |
| --- | --- | --- | ---: |
| `arducopter-quad` | `ArduCopter` | `quad` | 1 |
| `arduplane-plane` | `ArduPlane` | `plane` | 1 |

Generate the full catalog on demand with:

```bash
scripts/populate-config-bundles.py
```
